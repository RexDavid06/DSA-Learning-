'''Exercise 5: Password Validator

Create a PasswordValidator class.

Requirements:

Create a static method:

is_valid(password)

Return True if:

Password length is at least 8.
It contains at least one digit.

Otherwise return False.

Example:

print(PasswordValidator.is_valid("abc123"))
print(PasswordValidator.is_valid("hello1234"))

Expected output:

False
True

Expected understanding:

Static methods performing checks.'''